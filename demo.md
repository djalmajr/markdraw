# Markdraw Demo

This sample document is used for screenshots of the extension web mode.

## Highlights

- Markdown rendering
- Code highlighting
- Tables and block quotes
- Quick visual validation

## Example Table

| Platform | Package |
| --- | --- |
| macOS | `.dmg` |
| Linux | `.AppImage` / `.deb` |
| Windows | `.msi` / `.exe` |

## Code Sample

```ts
function greet(name: string) {
  return `Hello, ${name}`;
}
```

> You can open folders, local files, or remote URLs.
